     {
       "name": "Open Camera Build Environment (Universal)",
       "image": "mcr.microsoft.com/vscode/devcontainers/universal:2",

       "postCreateCommand": "sudo apt-get update && sudo apt-get install -y wget unzip && wget -q
 https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O /tmp/cmdline-tools.zip && sudo mkdir -p /usr/local/android-sdk && sudo unzip -q /tmp/cmdline-tools.zip -d /usr/local/android-sdk && sudo mv /usr/local/android-sdk/cmdline-tools /usr/local/android-sdk/latest && rm /tmp/cmdline-tools.zip && yes | sudo /usr/local/android-sdk/latest/bin/sdkmanager --licenses > /dev/null && sudo /usr/local/android-sdk/latest/bin/sdkmanager \"platforms;android-34\" \"build-tools;34.0.0\" \"platform-tools\" && sudo chown -R vscode /usr/local/android-sdk",

       "remoteEnv": {
         "ANDROID_HOME": "/usr/local/android-sdk",
         "PATH": "${containerEnv:PATH}:/usr/local/android-sdk/latest/bin:/usr/local/android-sdk/platform-tools"
       },

       "forwardPorts": [8080],
       "remoteUser": "vscode"
     }
